rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir acceso a todos los documentos mientras el usuario esté autenticado
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
} 